#include <stdio.h>
#include <stdlib.h>

int main(){

    char chapter[20] = "Arrays";
    int numbers[] = {2, 5, 19};
    printf("%d \n", numbers[1]);
    printf("this is %s section \n", chapter);

    

    return 0;
}